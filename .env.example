# Server Configuration
PORT=5000

# MongoDB Atlas Connection URI
# Replace with your actual connection string from MongoDB Atlas
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxxx.mongodb.net/urlshortener?retryWrites=true&w=majority
